# drag-handle-no-hover

Hovering the drag handle shows nothing. Upstream pops a tooltip and a background — noise on a handle
that already appears on hover.

Files:

- `src/webview/components/ui/block-draggable.tsx` — tooltip held closed, hover background transparent
