# fork-notice

A heading at the top of `README.md` saying this is a fork, linking to `fork/README.md`.

Upstream's README opens on the product. A reader landing on this branch needs to know, first line,
that the tree is not upstream's and where the fork's rules are written.

Files:

- `README.md` — the region
- `fork/README.md` — the target
