# hide-mark-menu

The horizontal three dots — keyboard input, superscript, subscript — are gone. The vertical three
dots, the appearance menu, stay the one menu. Upstream ships both.

Files:

- `src/webview/components/ui/fixed-toolbar-buttons-trimmed.tsx` — out of the top bar
- `src/webview/components/ui/floating-toolbar-buttons.tsx` — out of the selection toolbar
