# no-selection-popover

No toolbar floats over a selection — Turn into, marks, link, comment, suggest are gone from it. The
marks keep their shortcuts; the top bar keeps Turn into. Upstream shows it on every selection.

Files:

- `src/webview/components/editor/editor-kit.tsx` — `FloatingToolbarKit` out
