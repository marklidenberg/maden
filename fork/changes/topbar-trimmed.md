# topbar-trimmed

The bar keeps undo, redo, Turn into, table, insert image, insert file, the three dots. Upstream
ships ~30.

Source view goes with the rest — "Reopen Editor With…" is the way back.

Files:

- `src/webview/components/ui/fixed-toolbar-buttons-trimmed.tsx` — ours
- `src/webview/components/editor/plugins/fixed-toolbar-kit.tsx` — the import swapped
